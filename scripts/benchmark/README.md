# Benchmark Helpers

- [`run_hybrid_benchmark.sh`](/home/denys/hello-pqc/scripts/benchmark/run_hybrid_benchmark.sh): run the hybrid-only benchmark matrix on top of the shared campaign runner

The stable top-level runner remains
[`scripts/run_benchmark_campaign.sh`](/home/denys/hello-pqc/scripts/run_benchmark_campaign.sh).
Compatibility wrappers remain in [`scripts/`](/home/denys/hello-pqc/scripts)
for callers that still use the older paths.
