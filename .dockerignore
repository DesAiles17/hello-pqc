# Keep Docker build context minimal for faster transfers and better cache behavior.
*

# Rust sources required by the Dockerfile.
!Cargo.toml
!Cargo.lock
!src/**

# Container definitions.
!Dockerfile
!docker-compose.yml
